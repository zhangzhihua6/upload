const getters = {
    head_show: state => state.header.show,
    head_downIcon: state => state.header.downIcon,
    Authorization: state => state.header.Authorization,
    id: state => state.header.id,
}
export default getters;